# 修改记录

## 2026-03-29 18:20:18 - 初始化记录

### 本轮修改了什么
- 为 C6 polygon-first 重构创建当前任务文档目录。
- 记录了本次计划、需求和后续修改记录入口。

### 为什么改
- 让本轮 C6 重构符合项目新的 AI 工作流文档要求。

### 改到了哪些模块或文档
- `dev_docs/current_20260329_182018/计划文档.md`
- `dev_docs/current_20260329_182018/需求文档.md`
- `dev_docs/current_20260329_182018/修改记录.md`

### 做了什么验证
- 已确认当前任务目标、边界和实现方向。

### 当前还剩什么问题
- 尚未开始实际代码重构。

## 2026-03-29 18:28:40 - C6 polygon-first 核心重构完成

### 本轮修改了什么
- 将 C6 的决策输入从 `mask_bbox` 改为 `polygon_reference_bbox + polygon_ascii_map + boundary_notes`。
- 在 `CityC6Stages.generate(...)` 中新增 ownership 优先路径，让 C6 可以基于 `C3 ownership` 生成 polygon-first area 索引。
- 保留 `footprint guidance`，但从 C6 接受条件里移除了 `structure_fit` 的硬阻断作用。
- 将 `CityC6Validation` 的接受标准改为以 polygon 覆盖率和总面积比例为主。
- 更新了 C6 相关预览语义：
  - `C6_polygon_preview`
  - `polygon_overview`
  - legend 中明确 `polygon_first` 与“非最终可建造性判定”语义
- 同步更新 `CityController` 的 C6 prepare 链路，使其把 ownership 数据传入 C6 生成过程。
- 更新了 C6 工作流文档说明和 C6 单元测试断言。

### 为什么改
- 避免 bbox/mask 提前把城市空间拉成方框。
- 让 C6 真正服从 polygon 语义，而不是被 C1 buildableGroups 当作最终合法边界限制死。
- 把“是否最终能建”交回后续 C7/C8/C9 和结构本身，而不是让 C6 提前裁决。

### 改到了哪些模块或文档
- `src/main/java/com/user/terra_script/world/city/stage/c6/CityC6Stages.java`
- `src/main/java/com/user/terra_script/world/city/stage/c6/CityC6Validation.java`
- `src/main/java/com/user/terra_script/world/city/stage/c6/CityC6BuildAreaPreviewExporter.java`
- `src/main/java/com/user/terra_script/world/city/stage/c6/CityC6GroupPreviewExporter.java`
- `src/main/java/com/user/terra_script/server/mcp/CityController.java`
- `src/test/java/com/user/terra_script/world/city/stage/c6/CityC6ValidationTest.java`
- `docs/workFlow/C6_可建造矩形生成.md`

### 做了什么验证
- 已通过：
  - `./gradlew test --tests "com.user.terra_script.world.city.stage.c6.CityC6ValidationTest"`
  - `./gradlew compileJava`

### 当前还剩什么问题
- 运行中的游戏进程尚未重新加载这次 C6 代码，尚未做在线 `city_c6_rect_prepare` 现场验证。
- `bbox` 仍作为内部预览 framing 和兼容字段存在，但已不再作为主设计语义。

## 2026-03-29 20:17:55 - C8/C9 去 BBox 化落地中

### 本轮修改了什么
- 在 `C8` 中新增基于 `C6 indexByBlock` 的 area geometry 上下文，改为按 polygon / area block 集合求解。
- 删除 `CityC8ArrangementEngine` 对 `area.bbox` 的主轴判断和越界判定依赖，改为整块 footprint in-area 校验。
- 将 `C8` 的 `terrain_impact_bbox` 改名为 `terrain_impact_extent`。
- 将 `C9` 的 enqueue / dry-run 入口改为先做 area block 复核，越界 placement 不再入队，并记录 `runtime_out_of_area`。
- 调整 `C8/C9` 预览导出链路，改为按 area geometry 取景和绘制 polygon area，而不是画 bbox 框。
- 补充 `C8/C9` 单元测试，覆盖 irregular area、主轴启发式和 runtime_out_of_area 场景。
- 更新当前任务的计划文档、需求文档，以及 C8/C9 工作流文档与测试清单中的 bbox 语义。

### 为什么改
- 让 polygon-first 不只停留在 C6 输入和校验层，而是一路延续到 C8 求解与 C9 落地。
- 避免 C8 继续被方形 bbox 拉歪，也避免 C9 把已经越出 polygon 的 placement 继续执行。

### 改到了哪些模块或文档
- `src/main/java/com/user/terra_script/world/city/stage/c8/*`
- `src/main/java/com/user/terra_script/world/city/stage/c9/*`
- `src/main/java/com/user/terra_script/world/city/stage/CityStagePreviewUtil.java`
- `src/main/java/com/user/terra_script/server/mcp/CityController.java`
- `src/test/java/com/user/terra_script/world/city/stage/c8/CityC8ArrangementEngineTest.java`
- `src/test/java/com/user/terra_script/world/city/stage/c9/CityC9StagesTest.java`
- `docs/workFlow/C8_排列坐标求解与基台计划.md`
- `docs/workFlow/C9_结构落地与装饰计划.md`
- `docs/TEST_C_FLOW_CHECKLIST.md`

### 做了什么验证
- 已通过：
  - `./gradlew test --tests "com.user.terra_script.world.city.stage.c8.CityC8ArrangementEngineTest"`
  - `./gradlew test --tests "com.user.terra_script.world.city.stage.c9.CityC9StagesTest"`
- `./gradlew compileJava --rerun-tasks`
- 在线运行态复验：
  - 重启游戏后执行 `city_c8_generate(city_3120_-3200, g_market_03)`，group 级 `c8_foundation.json` 已写出 `version=4` 和 `terrain_impact_extent`
  - 随后执行 `city_c9_generate(city_3120_-3200, g_market_03, mode=dry_run)`，`C9` 已优先消费 group 级 C8 结果
  - `c9_placement.json` 与 `c9_structure_preview.legend.json` 已不再混入旧队列状态，结果与本次 dry-run 一致

### 当前还剩什么问题
- 尚未执行 `apply_now` 真正落地方块，只完成了 dry-run 级别运行态验收。
- 仍未做整城全量 C8/C9 回归，只验证了目标 group 的优先级链路与预览语义。
