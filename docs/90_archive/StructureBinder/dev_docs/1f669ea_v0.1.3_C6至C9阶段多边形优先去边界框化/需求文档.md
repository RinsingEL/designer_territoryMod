# 本次需求文档

## 用户需求摘要
在已经完成的 C6 polygon-first 基础上，继续把 C8/C9 全链路落实到底，删除 C8/C9 中 bbox 对执行、校验、预览和对外命名的主导作用。

## 用户特别强调的约束
- 删除 `bbox` 作为设计边界的主导作用。
- 删除 `mask_bbox` / buildable mask 作为强约束。
- 保留 footprint guidance，但它只是结构尺寸建议。
- C8/C9 的边界合法性按“整块 footprint 在 polygon / area block 集合内”判定。
- `terrain_impact_bbox` 等对外字段也要一起去 bbox 化，不做兼容双写。

## 不做的事情
- 这次不重写 C7 决策模型。
- 这次不要求删掉 C6 内部兼容数据模型里的 `bbox` 字段本体。

## 当前工作边界
- 改 C8/C9 的 area geometry 消费方式、求解约束、运行时复核、预览取景和文档。
- 同步更新当前任务文档，不新建新的 `current_*` 目录。
