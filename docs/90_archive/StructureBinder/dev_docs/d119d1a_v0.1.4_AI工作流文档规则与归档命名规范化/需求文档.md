# 本次需求文档

## 用户需求摘要
把现有 `dev_docs` 升级成统一的 AI 工作流文档机制，并把规则写进 `AGENTS.md`。

## 用户特别强调的约束
- 每次计划模式结束、正式开工前，都要先创建当前任务文档目录。
- 必须写入：
  - `计划文档.md`
  - `需求文档.md`
  - `修改记录.md`
- 每次完成后都要更新 `修改记录.md`。
- 验收时必须回读这三份文档。
- 提交后必须把当前目录改名为 `git短提交号_版本号_中文标题`。

## 不做的事情
- 这次不改 `dev_docs` 根目录名。
- 这次不切换到 `AI_docs`。

## 当前工作边界
- 只改 `AGENTS.md` 和当前任务对应的 `dev_docs/current_20260329_181630/` 文档。
- 不在这一步顺带修改其他 skill、MCP 或运行时逻辑。
