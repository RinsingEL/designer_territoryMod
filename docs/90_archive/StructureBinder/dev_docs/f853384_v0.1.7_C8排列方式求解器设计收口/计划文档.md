# 本次任务标题

收口 C8 排列方式求解器的文档方案，并将当前讨论结论提交归档

## 最终计划摘要

将当前“排列方式枚举补洞”的讨论，提升为 `C8` 独立求解器系统的文档收口，在清单中补入独立任务，并整理本轮工作文档后完成提交，给后续从 `arrangement_type` 迁移到求解器描述留出清晰入口。

## 实施步骤

1. 复核现有 `C8` 改造案子和主模块字段设计，确认旧方案中的参数骨架。
2. 在清单中新增“排列方式求解器设计收口”独立任务，明确求解器方向、输入分层与待收口问题。
3. 更新本轮 `dev_docs/current_...` 工作文档，使计划、需求和修改记录与实际工作一致。
4. 复核文档改动范围，按项目提交规范完成中文提交。

## 验收标准

- 清单中已新增独立的 `C8` 排列方式求解器收口项。
- 文档中已明确求解器与旧 `arrangement_type` 的关系、分层输入和迁移问题。
- 本轮工作文档已与实际执行内容保持一致。
- 提交信息与提交正文符合项目中文提交规范。

## 关键假设

- 本轮先收文档方案和迁移方向，不直接落代码实现。
- 现有 `seed / limits / termination / strategy_params` 仍可作为后续升级求解器的参数骨架。
- 当前更适合把四类排列方式视为首批 preset，而不是最终通用分类。
