# 用户本次需求摘要

用户要求围绕 `dev_docs/current_20260330_181530/C8_C9_近期排期与需求清单.md` 中的“排列方式”部分继续收口讨论，并将“排列方式求解器”作为独立系统补进当前清单，随后把本轮文档修改提交。

## 用户特别强调的约束

- 使用中文对话。
- 文件默认按 UTF-8 打开和保存。
- 回复中的文件链接使用 markdown 绝对路径，格式固定为 `/c:/...`。
- 这次先聚焦“排列方式 / 求解器”本身，不扩散到其他未定规则。
- 讨论结束后，需要把当前文档改动一起提交。

## 不做的事情

- 不在本次直接完成 `C8` 求解器代码实现。
- 不把道路骨架、次级建筑 `single` 规则一并混入本次文档收口。
- 不直接把 `COURTYARD` 一步升级成完整“口字围合 / 空腔锚点”系统。

## 当前决定的工作边界

- 只处理 `C8` 排列方式 / 求解器相关文档和必要工作记录。
- 以清单新增独立任务的方式收口求解器方向，不单开新的工作流文档。
- 保持现有 `C7/C8/C9` 代码不变，本轮不重写落地执行链。
