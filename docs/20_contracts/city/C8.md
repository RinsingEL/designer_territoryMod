# C8 Contract

## 当前定位

`C8` 负责维护结构施工会话，而不是直接给出整图最终落地结果。

## 当前稳定输出重点

- `foreman_plan`
- `session_state`
- `active_node`
- `node_queue`
- `validated_nodes`
- `failed_attempts`
- `foundation_context`

## 当前稳定输入重点

- 来自 `C7` 的工头计划
- 当前节点决策：
  - `node_id`
  - `selected_template_id`
  - `selected_connector_dir`
  - `selected_rotation`
  - `x`
  - `z`
  - `terrain_relax_profile`

## 当前边界

- `C8` 负责节点决策会话与程序校验
- `C8` 不再承担整图复杂排列枚举
- `C8` 的输出需要被 `C9` 消费成可执行建造计划
